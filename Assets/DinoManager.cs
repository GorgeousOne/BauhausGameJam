using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Random = UnityEngine.Random;

public class DinoManager : MonoBehaviour {
    //[SerializeField] private float gameWidth = 10f * 16 / 9;
    //[SerializeField] private float gameHeight = 10;
    [SerializeField] private Vector2 spawn = new (0, 0);

    void Start()
    {
            
    }

    // Update is called once per frame
    void Update()
    {
           
    }

    private void _spawnRandom(GameObject dinoPrefab) {
        
    }
}
